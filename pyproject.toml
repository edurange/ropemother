[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "ropemother"
version = "0.1.0.dev7"
description = "A module for publish-subscribe broadcast messaging."
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    {name = "Joe Granville", email = "874605+jwgranville@users.noreply.github.com"}
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
]

[project.optional-dependencies]
zmq = [
    "pyzmq",
]
test = [
    "pytest",
]
dev = [
    "black",
    "build",
    "pylint",
    "pytest",
    "pyzmq",
]

[project.urls]
Source = "https://github.com/edurange/ropemother"
Issues = "https://github.com/edurange/ropemother/issues"

[tool.black]
line-length = 79
target-version = ["py313"]

[tool.pylint.main]
py-version = "3.13"